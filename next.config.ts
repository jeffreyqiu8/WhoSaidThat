import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  typescript: {
    // Only ignore type errors during build if you're confident in your types
    // ignoreBuildErrors: false,
  },
};

export default nextConfig;
