# Production Environment Variables for Vercel
# Copy these variable names to your Vercel project settings

# Upstash Redis (Required)
# Get from: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# Pusher Channels (Required)
# Get from: https://dashboard.pusher.com/
PUSHER_APP_ID=your_pusher_app_id
PUSHER_SECRET=your_pusher_secret_key

# Pusher Public Variables (Exposed to Browser)
NEXT_PUBLIC_PUSHER_APP_KEY=your_pusher_app_key
NEXT_PUBLIC_PUSHER_CLUSTER=us2

# Notes:
# - All variables are required for the app to function
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Server-side secrets (PUSHER_SECRET, UPSTASH_REDIS_REST_TOKEN) remain private
# - Set these in Vercel Dashboard → Project Settings → Environment Variables
